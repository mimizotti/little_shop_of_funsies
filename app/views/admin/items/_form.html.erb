<%= form_for([:admin, @item], url: admin_items_path, html: { multipart: true }) do |form| %>
  

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
		<%= form.label :image %>
    <%= form.file_field :image %>
  </div>
  
  <div class="category_select_box">
    <%= form.select(:category_id, @categories.map { |category| [category.title, category.id] }) %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>