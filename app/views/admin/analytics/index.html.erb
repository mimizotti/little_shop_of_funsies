<!DOCTYPE html>
<head>
	<meta charset="utf-8">
	<title>Analytics</title>
</head>
<body>
	<div class = "container">
    <center><h1> Analytics</h1></center><br>
    <div class="row text-center">
      <div class='col-6 card bg-success'>
        <div class='card-body '>
        <p><strong>Total shop sales to date: $ <%= @analytics.total_sales %>. <br>Total completed orders: <%= @analytics.completed_orders %></strong></p>
        </div>
      </div>
      <div class='col-6 card bg-primary'>
        <div class='card-body '>
          <p><strong> Average number of onsies per order: <%= @analytics.average_quantity_purchaced %></strong></p>
        </div>
      </div>
      <div class='col-6 card bg-warning'>
        <div class='card-body '>
          <p>Quantity ordered per item: <% @analytics.sales_by_item_title.each do |key, value| %>
          <%= key %>: <%= value  %><br>
          <% end %> </p>
        </div>
      </div>

<p>Number of orders each customer has placed: <%= @analytics.orders_per_customer %> </p>

<p>Number of items each customer has ordered: <%= @analytics.items_ordered_per_customer %>
    </div>
  </div>
</body>
